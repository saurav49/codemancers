const ReducerFn = (state, action) {
    switch (action.type) {
        case "ADD__POST":
            return {
                ...state,
            }
            break;
    
        default:
            break;
    }
}